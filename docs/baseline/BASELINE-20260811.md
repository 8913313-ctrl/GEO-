# 桐灼 GEO 产品化开发基线

记录日期：2026-08-11  
任务编号：P0-T01  
仓库：`D:\Backup\Documents\ChatGPT\灼见geo\GEO-`

## Git 基线

- 当前分支：`codex/tongzhuo-geo-update-20260809`
- 远程跟踪：`origin/codex/tongzhuo-geo-update-20260809`
- 当前提交：`9ba26d2 chore: ignore local visual QA artifacts`
- 远程地址：`https://github.com/8913313-ctrl/GEO-.git`
- 基线检查时的未跟踪文件：`docs/GENERAL-ENTERPRISE-PROJECT-DEVELOPMENT-PLAN.md`

该未跟踪文件是本轮已确认的开发项目书，不是需要清理的运行产物。后续任务不得重置、覆盖或删除它。

## 本机运行时

- Node.js：`v26.1.0`
- npm：`11.13.0`
- 项目 `package.json` 要求 Node.js `>=24.0.0`，当前版本满足要求。

## 与本项目相关的监听服务

| 地址 | 端口 | PID | 进程 | 命令 |
|---|---:|---:|---|---|
| `0.0.0.0` | `18080` | `23668` | `node.exe` | `node --env-file=.env.lan site-server.mjs` |

`http://127.0.0.1:18080/` 基线响应：

- HTTP 状态：`200`
- 页面标题：`桐灼科技`
- 样式版本：`site-v8.css?v=20260811-passport30`

该官网服务在 P0-T01 执行前已经运行。本任务没有启动或停止它。后续任务需要重启时，必须先确认 PID，且只能处理明确属于本项目的进程。

另有一个与当前仓库无关的 Vite 进程监听 `127.0.0.1:34116`，PID 为 `28036`。不得在本项目任务中停止它。

## 当前定位基线

本项目不是普通企业管理系统，而是桐灼已经开发的 GEO 服务系统。产品化目标是把同一套 GEO 核心能力卖给建材、机械、医疗等行业客户：

```text
GEO 核心系统
+ 行业适配模板
+ 客户独立品牌与企业数据
= 可私有化交付的客户 GEO 项目
```

桐灼项目是第一套自用和示范实例；客户行业不是新的软件产品，GEO 也不是可选行业模板。

## P0-T01 验收结论

- [x] 当前分支与跟踪分支已确认。
- [x] 当前提交已确认。
- [x] 用户已有/本轮已有未提交文件已记录。
- [x] Node.js 与 npm 版本已确认。
- [x] 监听端口与相关进程已确认。
- [x] 现有官网服务健康响应已确认。
- [x] 未停止、删除或修改任何既有进程与业务代码。

下一任务：`P0-T02`，记录组件检查基线。

## P0-T02 检查基线

以下检查在 `tongzhuo-geo-platform-demo/` 执行：

| 检查 | 结果 |
|---|---|
| `npm run check:site` | 通过；CMS 发布契约、发布/撤回闭环、官网运行时、AI 抓取准备检查均通过 |
| `node scripts/check-ai-generation-service.mjs` | 通过 |
| `node scripts/check-planning-generation-flow.mjs` | 通过 |
| `node scripts/check-citation-visibility.mjs` | 通过 |

本次没有触发 `CITATION_RESEARCH_NOT_INSTALLED`。上述四项是后续产品化改动必须保持的最小回归基线。

P0 阶段已完成。下一阶段：`P1-T01`，逐组件启动与职责核对。
