# 凭据轮换记录

## 2026-07-26：演示环境凭据失效处理

- 已清空演示后台的模型供应商配置。
- 已清空发布器设备 Token、Device Secret、配对记录和本地发布器 API Token。
- 已关闭发布器自动执行，并切换为草稿模式。
- 运行数据、浏览器 Profile、安装包和备份均被 Git 忽略，不能提交到正式仓库。

## 仍需在外部平台完成的动作

- 在 DeepSeek 控制台删除旧 API Key，并新建一个替代 Key。
- 新 Key 只在正式服务端的加密凭据存储完成后录入；不得重新写入 JSON、前端代码、安装包或文档。
- 正式发布器上线后重新配对设备，服务端只保存设备凭据的哈希或加密引用，不保存可直接复用的明文 Secret。

本文件不记录任何 Key、Token、设备标识或账号信息。
