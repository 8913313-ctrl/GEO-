@extends('admin.layouts.app')

@section('content')
<div class="mx-auto max-w-[1500px] space-y-5">
    <section class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
        <div class="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between">
            <div>
                <div class="text-xs font-black tracking-[0.18em] text-blue-600">WEBSITE FAQ</div>
                <h1 class="mt-1 text-2xl font-black text-slate-950">问题地图</h1>
                <p class="mt-2 max-w-3xl text-sm leading-6 text-slate-500">这是官网的动态问答内容库。先建立分类，再新增问题；发布后会直接出现在官网问题地图，并带有适合AI抓取的问答结构化数据。</p>
            </div>
            <a href="{{ url('/issues.html') }}" target="_blank" rel="noreferrer" class="inline-flex items-center justify-center gap-2 rounded-lg border border-slate-200 px-4 py-2.5 text-sm font-bold text-slate-700 hover:bg-slate-50"><i data-lucide="external-link" class="h-4 w-4"></i>预览官网问题地图</a>
        </div>
    </section>

    <section class="grid gap-5 xl:grid-cols-[360px_minmax(0,1fr)]">
        <div class="space-y-5">
            <form method="POST" action="{{ route('admin.tongzhuo-cms.faqs.categories.store') }}" class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
                @csrf
                <div class="flex items-center gap-2"><i data-lucide="folder-plus" class="h-5 w-5 text-blue-600"></i><h2 class="font-black text-slate-950">新增问题分类</h2></div>
                <p class="mt-2 text-sm leading-6 text-slate-500">例如：GEO优化、短视频运营、企业AI落地。</p>
                <label class="mt-4 block text-sm font-bold text-slate-700">分类名称<input name="name" required maxlength="80" class="mt-2 block w-full rounded-lg border-slate-200 text-sm" placeholder="例如：GEO优化"></label>
                <label class="mt-4 block text-sm font-bold text-slate-700">分类地址标识 <span class="font-normal text-slate-400">可不填</span><input name="slug" maxlength="120" pattern="[a-z0-9-]+" class="mt-2 block w-full rounded-lg border-slate-200 font-mono text-sm" placeholder="例如：geo"></label>
                <label class="mt-4 block text-sm font-bold text-slate-700">分类简介<textarea name="description" rows="3" maxlength="240" class="mt-2 block w-full rounded-lg border-slate-200 text-sm" placeholder="说明这一类问题解决什么。"></textarea></label>
                <label class="mt-4 block text-sm font-bold text-slate-700">排序<input name="sort_order" type="number" min="0" value="10" class="mt-2 block w-full rounded-lg border-slate-200 text-sm"></label>
                <button class="mt-5 inline-flex w-full items-center justify-center gap-2 rounded-lg bg-blue-600 px-4 py-2.5 text-sm font-bold text-white hover:bg-blue-700"><i data-lucide="plus" class="h-4 w-4"></i>新增分类</button>
            </form>

            <div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
                <div class="flex items-center gap-2"><i data-lucide="folders" class="h-5 w-5 text-cyan-600"></i><h2 class="font-black text-slate-950">现有分类</h2></div>
                <div class="mt-4 space-y-3">@forelse($categories as $category)<form method="POST" action="{{ route('admin.tongzhuo-cms.faqs.categories.update', $category->id) }}" class="rounded-lg border border-slate-200 bg-slate-50 p-3">@csrf @method('PUT')<div class="flex items-center justify-between gap-2"><input name="name" value="{{ $category->name }}" required maxlength="80" class="w-full rounded-md border-slate-200 bg-white text-sm font-bold"><span class="shrink-0 text-xs text-slate-400">{{ $category->items_count }}题</span></div><input name="description" value="{{ $category->description }}" maxlength="240" class="mt-2 block w-full rounded-md border-slate-200 bg-white text-xs" placeholder="分类简介"><div class="mt-2 flex items-center gap-2"><label class="flex items-center gap-1.5 text-xs font-bold text-slate-600"><input name="is_visible" type="checkbox" value="1" @checked($category->is_visible) class="rounded border-slate-300 text-blue-600">官网显示</label><input name="sort_order" type="number" min="0" value="{{ $category->sort_order }}" class="ml-auto w-16 rounded-md border-slate-200 bg-white text-xs"><button class="rounded-md px-2 py-1 text-xs font-bold text-blue-600 hover:bg-blue-50">保存</button></div></form>@empty<p class="text-sm text-slate-500">还没有分类。</p>@endforelse</div>
            </div>
        </div>

        <div class="space-y-5">
            <form method="POST" action="{{ route('admin.tongzhuo-cms.faqs.items.store') }}" class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
                @csrf
                <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><div class="flex items-center gap-2"><i data-lucide="circle-help" class="h-5 w-5 text-blue-600"></i><h2 class="font-black text-slate-950">新增问题</h2></div><p class="mt-1 text-sm text-slate-500">用客户会直接搜索或询问AI的自然语言写问题，答案保持清楚、具体。</p></div><div class="flex rounded-lg border border-slate-200 p-1"><label class="cursor-pointer rounded-md px-3 py-1.5 text-xs font-bold text-slate-600 has-[:checked]:bg-slate-950 has-[:checked]:text-white"><input class="sr-only" name="status" type="radio" value="draft" checked>存为草稿</label><label class="cursor-pointer rounded-md px-3 py-1.5 text-xs font-bold text-slate-600 has-[:checked]:bg-emerald-600 has-[:checked]:text-white"><input class="sr-only" name="status" type="radio" value="published">直接发布</label></div></div>
                <div class="mt-5 grid gap-4 md:grid-cols-2"><label class="text-sm font-bold text-slate-700">所属分类<select name="category_id" required class="mt-2 block w-full rounded-lg border-slate-200 text-sm"><option value="">请选择分类</option>@foreach($categories as $category)<option value="{{ $category->id }}">{{ $category->name }}</option>@endforeach</select></label><label class="text-sm font-bold text-slate-700">问题排序<input name="sort_order" type="number" min="0" class="mt-2 block w-full rounded-lg border-slate-200 text-sm" placeholder="数值越小越靠前"></label><label class="text-sm font-bold text-slate-700 md:col-span-2">问题<input name="question" required maxlength="220" class="mt-2 block w-full rounded-lg border-slate-200 text-sm" placeholder="例如：GEO优化是不是只改官网关键词？"></label><label class="text-sm font-bold text-slate-700 md:col-span-2">答案<textarea name="answer" required rows="7" maxlength="8000" class="mt-2 block w-full rounded-lg border-slate-200 text-sm leading-6" placeholder="请直接回答问题，说明判断条件、做法或注意事项。"></textarea></label><label class="text-sm font-bold text-slate-700">摘要 <span class="font-normal text-slate-400">可不填</span><textarea name="excerpt" rows="3" maxlength="300" class="mt-2 block w-full rounded-lg border-slate-200 text-sm" placeholder="用于搜索结果摘要；不填将自动提取答案前段。"></textarea></label><label class="text-sm font-bold text-slate-700">独立链接标识 <span class="font-normal text-slate-400">可不填</span><input name="slug" pattern="[a-z0-9-]+" maxlength="140" class="mt-2 block w-full rounded-lg border-slate-200 font-mono text-sm" placeholder="例如：what-is-geo"></label><details class="md:col-span-2"><summary class="cursor-pointer text-sm font-bold text-slate-600">SEO设置（可选）</summary><div class="mt-3 grid gap-4 md:grid-cols-2"><label class="text-sm font-bold text-slate-700">SEO标题<input name="seo[title]" maxlength="180" class="mt-2 block w-full rounded-lg border-slate-200 text-sm"></label><label class="text-sm font-bold text-slate-700">SEO描述<input name="seo[description]" maxlength="300" class="mt-2 block w-full rounded-lg border-slate-200 text-sm"></label></div></details></div>
                <button class="mt-5 inline-flex items-center justify-center gap-2 rounded-lg bg-slate-950 px-4 py-2.5 text-sm font-bold text-white hover:bg-blue-700"><i data-lucide="save" class="h-4 w-4"></i>保存问题</button>
            </form>

            <section class="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div class="flex items-center justify-between border-b border-slate-100 px-5 py-4"><div><h2 class="font-black text-slate-950">问题库</h2><p class="mt-1 text-sm text-slate-500">编辑、发布和下线已有问题。</p></div><span class="text-sm font-bold text-slate-500">{{ $items->total() }} 条</span></div><div class="divide-y divide-slate-100">@forelse($items as $item)<details class="group"><summary class="flex cursor-pointer list-none items-center gap-3 px-5 py-4 hover:bg-slate-50"><span class="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg {{ $item->status === 'published' ? 'bg-emerald-50 text-emerald-600' : 'bg-amber-50 text-amber-600' }}"><i data-lucide="{{ $item->status === 'published' ? 'check' : 'file-pen-line' }}" class="h-4 w-4"></i></span><span class="min-w-0 flex-1"><span class="block truncate font-bold text-slate-950">{{ $item->question }}</span><span class="mt-1 block text-xs text-slate-500">{{ $item->category?->name }} · {{ $item->status === 'published' ? '已发布' : '草稿' }}</span></span><i data-lucide="chevron-down" class="h-4 w-4 text-slate-400 transition group-open:rotate-180"></i></summary><div class="border-t border-slate-100 bg-slate-50 p-5"><form method="POST" action="{{ route('admin.tongzhuo-cms.faqs.items.update', $item->id) }}">@csrf @method('PUT')<div class="grid gap-4 md:grid-cols-2"><label class="text-sm font-bold text-slate-700">分类<select name="category_id" class="mt-2 block w-full rounded-lg border-slate-200 text-sm">@foreach($categories as $category)<option value="{{ $category->id }}" @selected($item->category_id === $category->id)>{{ $category->name }}</option>@endforeach</select></label><label class="text-sm font-bold text-slate-700">排序<input name="sort_order" type="number" min="0" value="{{ $item->sort_order }}" class="mt-2 block w-full rounded-lg border-slate-200 text-sm"></label><label class="text-sm font-bold text-slate-700 md:col-span-2">问题<input name="question" value="{{ $item->question }}" required maxlength="220" class="mt-2 block w-full rounded-lg border-slate-200 text-sm"></label><label class="text-sm font-bold text-slate-700 md:col-span-2">答案<textarea name="answer" required rows="6" maxlength="8000" class="mt-2 block w-full rounded-lg border-slate-200 text-sm leading-6">{{ $item->answer }}</textarea></label><label class="text-sm font-bold text-slate-700">摘要<textarea name="excerpt" rows="3" maxlength="300" class="mt-2 block w-full rounded-lg border-slate-200 text-sm">{{ $item->excerpt }}</textarea></label><label class="text-sm font-bold text-slate-700">链接标识<input name="slug" value="{{ $item->slug }}" pattern="[a-z0-9-]+" maxlength="140" class="mt-2 block w-full rounded-lg border-slate-200 font-mono text-sm"></label></div><div class="mt-4 flex flex-wrap items-center gap-2"><select name="status" class="rounded-lg border-slate-200 text-sm"><option value="draft" @selected($item->status === 'draft')>草稿</option><option value="published" @selected($item->status === 'published')>已发布</option></select><button class="rounded-lg bg-slate-950 px-3 py-2 text-sm font-bold text-white">保存修改</button></form><form method="POST" action="{{ route('admin.tongzhuo-cms.faqs.items.delete', $item->id) }}" onsubmit="return confirm('确认删除这条问题吗？')">@csrf<button class="rounded-lg px-3 py-2 text-sm font-bold text-rose-600 hover:bg-rose-50">删除</button></form></div></div></details>@empty<div class="px-5 py-10 text-center text-sm text-slate-500">还没有问题。请先新增一条，或从旧FAQ内容中补录。</div>@endforelse</div>@if($items->hasPages())<div class="border-t border-slate-100 px-5 py-4">{{ $items->links() }}</div>@endif</section>
        </div>
    </section>
</div>
@endsection
